# VARUNA backend + ingestion. Python 3.11 target (works on 3.13 locally).
# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# --- Google Cloud ---
google-genai==0.8.0
google-adk==0.1.0
google-cloud-bigquery==3.27.0
google-cloud-firestore==2.19.0
db-dtypes==1.3.1              # bigquery -> pandas

# --- Data / ingestion ---
pandas==2.2.3
pyyaml==6.0.2
requests==2.32.3
rapidfuzz==3.10.1            # fuzzy ward-name matching
shapely==2.0.6              # geometry handling for wards
pyproj==3.7.0

# --- RAG / SOP ingestion ---
chromadb==0.5.23
pypdf==5.1.0

# --- ML fallback (local LightGBM, per CLAUDE.md 8a) ---
lightgbm==4.5.0
scikit-learn==1.6.0
