# VARUNA backend runtime (Cloud Run container). Pinned to validated versions.
# Ingestion/ML deps are in requirements-ingest.txt (not needed at runtime).
fastapi==0.139.0
uvicorn[standard]==0.50.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
PyYAML==6.0.3

# Google Cloud (AI Studio Gemini, BigQuery, Firestore) — no Vertex endpoints (§6)
google-genai==2.10.0
google-cloud-bigquery==3.42.1
google-cloud-firestore==2.28.0
google-adk==2.3.0            # ADK agents (RiskAnalyst, ResponsePlanner)
