# Data foundation + ML pipeline deps (local/CI only; NOT in the runtime container).
# Install: pip install -r requirements-ingest.txt
requests==2.32.3
pandas==2.2.3
PyYAML==6.0.3
rapidfuzz==3.10.1        # fuzzy ward-name matching
shapely==2.0.6           # ward KML -> geometry, point-in-polygon
google-cloud-bigquery==3.42.1
db-dtypes==1.3.1         # bigquery -> pandas (eval helpers)

# Optional local ML fallback (§8a) — only if BQML is unavailable
# lightgbm==4.5.0
# scikit-learn==1.6.0
